# Local signing + notarization settings for macOS builds
# Copy to: .env.notarize (DO NOT COMMIT)

# Optional: let Tauri notarize the .app during bundling (removes the "skipping app notarization" warning)
# These are only used if you have a Developer ID setup.
APPLE_ID=your-apple-id@example.com
APPLE_TEAM_ID=YOURTEAMID
# Create an app-specific password at https://appleid.apple.com
APPLE_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Used by our DMG notarization step (create with: xcrun notarytool store-credentials)
APPLE_KEYCHAIN_PROFILE=your-app-notarize

# Code signing identity for your Developer ID certificate
# Find yours with: security find-identity -v -p codesigning
APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID)"

# Set to 1 to enable notarization
NOTARIZE_TAURI_APP=1
NOTARIZE_DMG=1
